const { add, substract } = require("./module/utils/index"); // ai khne utils ar pore index file na likhleo same output dekhabe. by default require function utils folder ar moddhe  a index.js file e khujbe.

console.log(add(1, 2)); // 3
console.log(substract(5, 3)); // 2
